# Replace with your local machine's IP address (not localhost) when testing on a physical device
# Find your IP: macOS → `ipconfig getifaddr en0` | Windows → `ipconfig` | Linux → `hostname -I`
# For iOS/Android Simulator you can use localhost
EXPO_PUBLIC_BACKEND_WS_URL=ws://192.168.1.X:4000/interview
